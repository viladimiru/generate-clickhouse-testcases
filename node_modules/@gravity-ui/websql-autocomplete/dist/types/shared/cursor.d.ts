import { Token, TokenStream } from 'antlr4ng';
import { ICandidateRule } from 'antlr4-c3';
import { CursorPosition } from './autocomplete-types';
export interface TokenPosition {
    startLine: number;
    startColumn: number;
    endLine: number;
    endColumn: number;
}
export declare const lineSeparatorRegex: RegExp;
export declare function getTokenPosition(token: Token, whitespaceToken: number): TokenPosition;
export declare function findCursorTokenIndex(tokenStream: TokenStream, cursor: CursorPosition, whitespaceToken: number, actualIndex?: boolean): number | undefined;
export declare function getCursorIndex(query: string, cursor: CursorPosition): number;
export declare function isStartingToWriteRule(cursorTokenIndex: number, rule: ICandidateRule): boolean;
