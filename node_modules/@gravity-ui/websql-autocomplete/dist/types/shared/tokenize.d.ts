import { LexerConstructor, ParserSyntaxError } from './autocomplete-types';
import { Lexer as LexerType } from 'antlr4ng';
export type Token = {
    ruleName: string;
    startIndex: number;
    type: number;
    text?: string;
};
export type TokenizeResult = {
    tokens: Token[];
    errors: ParserSyntaxError[];
};
export declare function tokenize<L extends LexerType>(Lexer: LexerConstructor<L>, symbolicNames: (string | null)[], whitespaceToken: number, query: string): TokenizeResult;
